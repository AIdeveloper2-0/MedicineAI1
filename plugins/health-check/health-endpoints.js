// health-endpoints.js
// Health endpoints disabled.
// Emergent sistemi kaldırıldığı için bu servis kapıları kapatıldı.

function setupHealthEndpoints(devServer, healthPlugin) {
  // Hiçbir endpoint (API) kurmuyoruz.
  // Sitenin arkasında gereksiz çalışan servisler kapatıldı.
  // Artık sadece senin uygulaman çalışıyor.
}

module.exports = setupHealthEndpoints;