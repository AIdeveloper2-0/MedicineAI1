// dev-server-setup.js
// Temizlenmiş dev server ayarı.
// Dışarıdan müdahale ve Emergent bağlantıları kaldırıldı.

function setupDevServer(config) {
  // Hiçbir özel ayar yapmadan config'i olduğu gibi geri döndürüyoruz.
  return config;
}

module.exports = setupDevServer;